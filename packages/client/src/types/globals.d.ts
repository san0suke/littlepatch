/**
 * Constantes trocadas em tempo de build pelo Vite (`define`, em `vite.config.ts`).
 *
 * Não é variável de ambiente: `import.meta.env` só enxerga o que começa com
 * `VITE_`, e a versão vem do `package.json` do próprio pacote.
 */
declare const __APP_VERSION__: string;
